# TODO: open items for the `open-pr` skill

Open follow-ups for the PR-opening and Test-plan-running workflow.

## 1. `tea` is installed but not logged in

`tea logins list` returned an empty table. If you want CLI-driven PR creation on Codeberg in future sessions, run `tea login add` once. The web UI works fine in the meantime. (Carried over from the old `commit-push-pr/TODO.md`.)
